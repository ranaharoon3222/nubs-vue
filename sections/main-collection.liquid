<div class="container">
  <h1 class="text-2xl md:text-3xl font-bold text-center mb-6 md:mb-16">
    {{ collection.title }}
  </h1>

  {% if collection.description != blank %}
    <p class="max-w-screen-sm mx-auto md:-mt-6 mb-6 md:mb-12">
      {{ collection.description }}
    </p>
  {% endif %}


</div>




   <div data-sectionId="{% if section.id %}{{section.id}}{% endif %}" data-settings='{% if section.settings %}{{section.settings | json}}{% endif %}' id="gf-products" style="display:grid" class="grid grid-cols-2 grid-rows-1 gap-0 overflow-hidden lg:grid-cols-3">                       
                              
                            </div>